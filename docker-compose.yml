version: '3.8'

services:
  email-parser-api:
    build: .
    container_name: email-parser-api
    ports:
      - "8000:8000"
    volumes:
      - ./app/config:/app/config:ro
    restart: unless-stopped
